const Discord = require("discord.js");
const myid = [''] // id xot da bne 
const polat = new Discord.Client();
const polat2 = new Discord.Client();
const polat3 = new Discord.Client();
const polat4 = new Discord.Client();
const polat5 = new Discord.Client();
const polat6 = new Discord.Client();
const polat7 = new Discord.Client();
const polat8 = new Discord.Client();
const polat9 = new Discord.Client();
const polat10 = new Discord.Client();

///////// id vc dane hata bcheta vc 


polat.on("ready", () => {
  var join = polat.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat2.on("ready", () => {
  var join = polat2.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat3.on("ready", () => {
  var join = polat3.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat4.on("ready", () => {
  var join = polat4.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat5.on("ready", () => {
  var join = polat5.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat6.on("ready", () => {
  var join = polat6.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat7.on("ready", () => {
  var join = polat7.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat8.on("ready", () => {
  var join = polat8.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat9.on("ready", () => {
  var join = polat9.channels.get(""); /// id voice
  if (join) join.join();
});
///////////////////
polat10.on("ready", () => {
  var join = polat10.channels.get(""); /// id voice
  if (join) join.join();
});
//////// playing or dnd  streaming ////////

polat.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat2.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat2.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat3.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat4.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat5.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat2.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat6.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [`polat bot`];
  setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat7.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [` polat bot`];
   setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat8.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [` polat bot`];
   setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat9.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [` polat bot`];
   setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});
//////// playing or dnd  streaming ////////

polat10.on("ready", () => {
  console.log(`dmd In Servers`);
  let statuses = [` polat bot`];
   setInterval(function() {
    let STREAMING = statuses[Math.floor(Math.random() * statuses.length)];
    polat3.user.setActivity(STREAMING, {
      type: "playing",
      url: "https://www.twitch.tv/faith"
    });
  }, 2000);
});

////// spam krdn ba acc

polat.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
////

polat2.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
///////
polat3.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});

////////

polat4.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});


//////

polat5.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});


///////
polat6.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
///////
polat7.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
///////
polat8.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
///////
polat9.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});
///////
polat10.on('message', message => {
if (message.content === 'owo') {
      let count = 0;
      let ecount = 0;
      for(let x = 0; x < 9000; x++) {
        message.channel.send(`owo daily`)
          .then(m => {
            count++;
          })
          
        }
      }
});

polat.login("");
polat2.login("");
polat3.login("");
polat4.login("");
polat5.login("");
polat6.login("");
polat7.login("");
polat8.login("");
polat9.login("");
polat10.login("");
